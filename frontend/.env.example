# No API URL needed — frontend and backend are on the same Vercel deployment
# Only needed if you add direct Supabase client-side queries later
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key
